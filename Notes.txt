# -------------- 18 - 05 - 2026 ------ # 
# TO DO's order
1. Backend --> Python code to perform a 3-way anova 
2. Test script to define the best inputs and expected outputs
3. Move to a front end --> probably streamlit
4. Test
5. Ponder about a db inclusion


# -------------- 22 - 05 - 2026 ------ #  
-- dependency checker
Dovrebbe controllare se nella stessa colonna ci sono nomi ripetuti
Se ci sono, dovrebbe controllare per quali valori differiscono e quali no 
L'idea è spottare i topi che sono ripetuti nel tempo e printare 
"I tuoi campioni sembrano dipendenti dalla variabile XXX" 
1. Forse si potrebbe controllare direttamente solo colonne con nomi specifici tipo sample/names/mouse/reps
2. Inutile che controlli misure e variabili dicotomiche

# possible imports to run mixed models

import pandas as pd  #trivial
import statsmodels.api as sm
import statsmodels.formula.api as smf

from statsmodels.stats.anova import anova_lm


# -------------- 29 - 06 - 2026 ------ #  
1. gli input funzionano ora, non trovo un modo migliore per chiederli al momento
2. da checkare se read_table viene usato effettivamente
3. prossima volta lavoro su possibile grafico e tabella con output


# -------------- 02 - 07 - 2026 ------ # 
Tested read_table on different extensions. Seems to work for now.

# -------------- 16 - 07 - 2026 ------ # 
1. Il template data è in formato long va creato uno script per trasformare l'excel/csv di input in formato long
2. Da implementare i grafici 
3. Secondo me c'è un loop di troppo e si può scrivere meglio la parte di parametri+run analysis
4. IMPORTANTE --> dependency check: utile non solo per mixed model ma anche per capire il tipo di t test ecc.